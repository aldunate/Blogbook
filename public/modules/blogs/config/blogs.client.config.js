
/*'use strict';

// Configuring the Articles module
angular.module('blogs').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		Menus.addMenuItem('topbar', 'Blogs', 'blogs', 'dropdown', '/blogs(/create)?', false);
		Menus.addSubMenuItem('topbar', 'blogs', 'List Blogs', 'blogs',false,false);
		Menus.addSubMenuItem('topbar', 'blogs', 'New Blog', 'blogs/create',false,false);
	}
]);*/
